<section class="container">
  <h3 class="shop-name">{{category.category_name}}</h3>

  <section class="flex-row justify-center">
    {{#each category.products as |product|}}
    {{> category-details}}
    {{/each}}
  </section>
</section>