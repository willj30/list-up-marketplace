<section class="container">
  <h3 class="shop-name">{{shop.name}}</h3>

  <section class="flex-row justify-center">
    {{#each shop.items as |items|}}
    {{> item-details}}
    {{/each}}
  </section>
</section>